{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the MUZERO main window: HTTP to local + BYOK APIs, OS info, dialogs.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "os:default",
    "dialog:default",
    "fs:default",
    "opener:allow-open-url",
    {
      "identifier": "http:default",
      "comment": "BYOK: LLM DJ + cloud music API are user-configured, so allow any https host. localhost kept for dev.",
      "allow": [{ "url": "https://**" }, { "url": "http://localhost:*" }, { "url": "http://127.0.0.1:*" }]
    }
  ]
}
